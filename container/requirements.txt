# Dependencies for container workers

# Environment variables
python-dotenv==1.0.1

# Database
asyncpg==0.30.0

# Browser automation
playwright==1.49.1

# Avito library dependencies (кэшируются; сама библиотека ставится отдельным слоем в Dockerfile)
beautifulsoup4>=4.12.0
lxml>=5.0.0
numpy>=1.26.0
opencv-python-headless>=4.8.0
httpx>=0.25.0

requests==2.32.3

# Async HTTP client for AI providers
aiohttp==3.11.11
