# Deployment / Развертывание

Автоматическое развертывание проекта Avito Parser на множество серверов через GitHub Actions.

## Быстрый старт

### 1. Настройка конфигурации серверов

Создайте JSON конфигурацию на основе шаблона [servers.example.json](servers.example.json):

```json
{
  "servers": [
    {
      "name": "server-1",
      "host": "89.34.219.71",
      "user": "root",
      "password": "your_actual_password_here",
      "port": 22
    },
    {
      "name": "server-2",
      "host": "89.34.219.72",
      "user": "root",
      "password": "your_actual_password_here",
      "port": 22
    }
  ]
}
```

**ВАЖНО:** Не коммитьте этот JSON с реальными паролями в Git!

### 2. Добавление конфигурации в GitHub Secrets

1. Перейдите в репозиторий на GitHub
2. **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Создайте секрет:
   - **Name:** `SERVERS_CONFIG`
   - **Value:** Вставьте весь JSON с вашими серверами и паролями

### 3. Готово!

Теперь при каждом `git push origin main` код автоматически развернется на всех серверах из конфигурации.

---

## Структура папки

```
deployment/
├── servers.example.json     # Шаблон конфигурации серверов (коммитится в Git)
├── workflow.yml             # GitHub Actions workflow
├── deploy.sh                # Скрипт развертывания (выполняется на сервере)
├── bootstrap.sh             # Скрипт первичной настройки сервера
└── README.md                # Эта инструкция
```

**Примечание:** Все дефолтные значения конфигурации находятся в `container/config.py`.

---

## Как это работает

```
1. git push origin main
        ↓
2. GitHub Actions запускается автоматически
        ↓
3. Читает SERVERS_CONFIG из GitHub Secrets
        ↓
4. Параллельно подключается ко всем серверам (через sshpass + пароли)
        ↓
5. На каждом сервере выполняет deployment/deploy.sh:
   - git pull
   - Проверка изменений
   - docker compose down (graceful)
   - Rebuild (если нужно)
   - docker compose up
   - Health check
        ↓
6. Если что-то сломалось → автоматический rollback
        ↓
7. Уведомление: ✅ success или ❌ failure
```

---

## Управление серверами

### Добавить новый сервер

1. Откройте GitHub → Settings → Secrets → `SERVERS_CONFIG`
2. Добавьте новый объект в массив `servers`:
   ```json
   {
     "name": "server-5",
     "host": "89.34.219.75",
     "user": "root",
     "password": "password",
     "port": 22
   }
   ```
3. Сохраните
4. При следующем push деплой произойдет на все серверы включая новый

### Удалить сервер

1. Откройте GitHub → Settings → Secrets → `SERVERS_CONFIG`
2. Удалите объект сервера из массива `servers`
3. Сохраните

### Временно отключить сервер

**Вариант 1:** Закомментировать в JSON (не работает, JSON не поддерживает комментарии)

**Вариант 2:** Удалить из конфигурации временно

**Вариант 3:** Настроить отдельный workflow для разных окружений

---

## Настройка окружения на сервере

**Все дефолтные значения находятся в `container/config.py`** и работают из коробки.

Файл `.env` опциональный и нужен только если вы хотите переопределить дефолтные значения на конкретном сервере.

```bash
# SSH на сервер
ssh root@89.34.219.71

# Перейти в проект
cd /root/zam/zamer/container

# (Опционально) Создать .env для переопределения параметров
nano .env
```

Параметры которые можно переопределить через `.env`:
- `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD` - подключение к БД
- `TOTAL_BROWSER_WORKERS`, `TOTAL_VALIDATION_WORKERS` - количество воркеров

**Примечание:** AI параметры (API ключ Fireworks) захардкожены в `config.py` и не читаются из `.env`.

---

## Просмотр логов развертывания

### В GitHub

1. Перейдите в **Actions**
2. Выберите **Deploy to Production**
3. Кликните на последний запуск
4. Разверните шаги для просмотра логов каждого сервера

### На сервере

```bash
# SSH на сервер
ssh root@89.34.219.71

# Посмотреть логи контейнера
cd /root/zam/zamer/container
docker compose logs -f parser

# Последние 100 строк
docker compose logs --tail=100 parser

# Статус контейнера
docker compose ps
```

---

## Ручное развертывание

### Через GitHub UI

1. GitHub → **Actions**
2. **Deploy to Production**
3. **Run workflow** → выбрать ветку `main` → **Run workflow**

### Напрямую на сервере

```bash
# SSH на сервер
ssh root@89.34.219.71

# Перейти в проект
cd /root/zam/zamer

# Запустить скрипт развертывания
bash deployment/deploy.sh
```

---

## Rollback (откат изменений)

### Автоматический rollback

Скрипт `deploy.sh` автоматически откатывается к предыдущему коммиту если:
- Контейнер не запустился
- Health check провалился
- Критическая ошибка в процессе

### Ручной rollback через GitHub

```bash
# На локальной машине
git log --oneline           # Найти хороший коммит
git reset --hard <commit-hash>
git push origin main --force

# GitHub Actions автоматически развернет предыдущую версию
```

### Ручной rollback на сервере

```bash
# SSH на сервер
ssh root@89.34.219.71

cd /root/zam/zamer
git log --oneline           # Найти коммит
git reset --hard <commit-hash>

cd container/
docker compose down
docker compose up -d --build

# Проверка
docker compose ps
docker compose logs -f parser
```

---

## Troubleshooting

### ❌ Deployment fails: "sshpass: command not found"

**Причина:** GitHub Actions не установил sshpass

**Решение:** Проверьте что в `workflow.yml` есть шаг:
```yaml
- name: Install sshpass
  run: |
    sudo apt-get update
    sudo apt-get install -y sshpass
```

### ❌ Deployment fails: "Permission denied (publickey,password)"

**Причина:** Неверный пароль или пользователь

**Решение:**
1. Проверьте `SERVERS_CONFIG` в GitHub Secrets
2. Убедитесь что пароль и пользователь правильные
3. Попробуйте подключиться вручную: `ssh root@89.34.219.71`

### ❌ Container fails to start after deploy

**Причина:** Ошибка в коде или конфигурации

**Решение:**
```bash
# SSH на сервер
ssh root@89.34.219.71
cd /root/zam/zamer/container

# Посмотреть логи
docker compose logs --tail=100 parser

# Попробовать запустить без -d для просмотра ошибок
docker compose up
```

### ❌ Deployment succeeds but code not updated

**Причина:** Скрипт завершился раньше или rollback произошел

**Решение:**
```bash
# Проверить текущий коммит на сервере
ssh root@89.34.219.71 "cd /root/zam/zamer && git log -1 --oneline"

# Сравнить с ожидаемым
git log -1 --oneline

# Если не совпадают - запустить деплой вручную
ssh root@89.34.219.71 "cd /root/zam/zamer && bash deployment/deploy.sh"
```

---

## Безопасность

### ✅ Хорошие практики

1. **Пароли в Secrets:** Никогда не коммитьте пароли в Git
2. **Сильные пароли:** Используйте длинные и сложные пароли
3. **Ограничение доступа:** Ограничьте кто может редактировать Secrets
4. **Регулярная ротация:** Меняйте пароли периодически

### ⚠️ Внимание

- Пароли хранятся в GitHub Secrets в зашифрованном виде
- Но в процессе деплоя они передаются в `sshpass` как переменные окружения
- Для максимальной безопасности рекомендуется использовать SSH ключи (см. основной DEPLOYMENT.md)

---

## Альтернатива: SSH ключи

Если вы хотите более безопасный вариант без паролей, см. [DEPLOYMENT.md](../DEPLOYMENT.md) для инструкций по настройке SSH ключей.

**Преимущества SSH ключей:**
- Безопаснее (256-bit ключ vs пароль)
- Проще в долгосрочной перспективе
- Один ключ для всех серверов
- Легко отозвать доступ

---

## Дополнительные возможности

### Уведомления в Discord/Slack

Добавьте в `workflow.yml`:

```yaml
- name: Notify deployment
  if: always()
  run: |
    curl -H "Content-Type: application/json" \
         -d '{"content": "🚀 Deployment to ${{ matrix.server.name }}: ${{ job.status }}"}' \
         ${{ secrets.DISCORD_WEBHOOK }}
```

### Разные окружения (dev/staging/prod)

Создайте отдельные secrets:
- `SERVERS_CONFIG_DEV`
- `SERVERS_CONFIG_STAGING`
- `SERVERS_CONFIG_PROD`

И отдельные workflows для каждого.

---

## Поддержка

Полная документация: [DEPLOYMENT.md](../DEPLOYMENT.md)

Вопросы или проблемы? Создайте Issue в GitHub.
